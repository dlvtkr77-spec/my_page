"use strict";
document.addEventListener("DOMContentLoaded",()=>{const progress=document.querySelector(".progress"),header=document.querySelector("[data-header]"),links=[...document.querySelectorAll('.desktop-nav a')],sections=[...document.querySelectorAll('main section[id]')],items=document.querySelectorAll('.reveal'),tilt=document.querySelector('[data-tilt]'),frame=tilt?.querySelector('.portrait-frame');const update=()=>{const r=document.documentElement,h=r.scrollHeight-r.clientHeight;progress.style.width=`${h>0?r.scrollTop/h*100:0}%`;header.classList.toggle('scrolled',scrollY>24)};const ro=new IntersectionObserver((es,o)=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');o.unobserve(e.target)}}),{threshold:.13,rootMargin:'0px 0px -60px 0px'});items.forEach(i=>ro.observe(i));const so=new IntersectionObserver(es=>{const v=es.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio);if(!v.length)return;links.forEach(l=>l.classList.toggle('active',l.getAttribute('href')===`#${v[0].target.id}`))},{threshold:[.25,.5,.75],rootMargin:'-25% 0px -55% 0px'});sections.forEach(s=>so.observe(s));document.querySelectorAll('a[href^="#"]').forEach(l=>l.addEventListener('click',e=>{const t=document.querySelector(l.getAttribute('href'));if(!t)return;e.preventDefault();t.scrollIntoView({behavior:'smooth',block:'start'})}));if(tilt&&frame&&matchMedia('(pointer:fine)').matches){tilt.addEventListener('mousemove',e=>{const r=tilt.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;frame.style.transform=`rotateY(${x*7}deg) rotateX(${y*-7}deg) translateY(-4px)`});tilt.addEventListener('mouseleave',()=>frame.style.transform='rotateY(0) rotateX(0)')}addEventListener('scroll',update,{passive:true});addEventListener('resize',update);update()});
